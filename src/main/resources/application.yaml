spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ingeneo?serverTimezone=America/Bogota&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: dangeapp
  jpa:
    database-platform: org.hibernate.dialect.MySQL57InnoDBDialect
    hibernate:
      ddl-auto: create-drop
    generate-ddl: true      
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true 
      